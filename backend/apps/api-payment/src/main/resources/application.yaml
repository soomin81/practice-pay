spring:
  application:
    name: api-payment
  datasource:
    url: jdbc:mysql://localhost:3306/stablecoin_payment?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: verysecret

server:
  port: 8081

app:
  api-key:
    # 로컬 개발용 평문 placeholder다 — 실제 배포 전에는 환경변수/Secret Manager로
    # 옮겨야 한다(HmacApiKeySecretHasher의 KDoc 참고).
    pepper: dev-only-api-key-pepper-change-in-production
