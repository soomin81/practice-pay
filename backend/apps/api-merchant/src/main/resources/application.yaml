spring:
  application:
    name: api-merchant
  datasource:
    url: jdbc:mysql://localhost:3306/stablecoin_payment?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: verysecret

server:
  port: 8083

app:
  invitation-token:
    # 로컬 개발용 평문 placeholder다 — 실제 배포 전에는 환경변수/Secret Manager로
    # 옮겨야 한다(HmacInvitationTokenHasher의 KDoc 참고).
    pepper: dev-only-invitation-token-pepper-change-in-production
